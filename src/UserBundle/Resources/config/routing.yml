homepage:
    path:     /
    defaults: { _controller: UserBundle:Default:index }

signup:
    path:     /signup
    defaults: { _controller: UserBundle:Session:signup }

login:
    path:     /login
    defaults: { _controller: UserBundle:Session:login }

login_check:
    path: /login_check

logout:
    path: /logout





gamer_view:
    path:     /{id}
    defaults: { _controller: UserBundle:Default:view }
    requirements:
        id: \d+
        
gamer_add:
    path:      /add
    defaults:
        _controller: UserBundle:Default:add

gamer_edit:
    path:      /edit/{id}
    defaults:
        _controller: UserBundle:Default:edit
    requirements:
        id: \d+

gamer_delete:
    path:      /delete/{id}
    defaults:
        _controller: UserBundle:Default:delete
    requirements:
        id: \d+